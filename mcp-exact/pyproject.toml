[project]
name = "mcp-exact"
version = "0.1.0"
description = "MCP server exposing Exact ERP (Online/Globe/Financials) as tools"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.2.0",
  "requests>=2.31",
]
