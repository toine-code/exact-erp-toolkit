[project]
name = "mcp-business-insights"
version = "0.1.0"
description = "MCP server with finance/operations insights on top of Exact ERP"
requires-python = ">=3.10"
dependencies = ["mcp>=1.2.0", "requests>=2.31"]
